/**
 * Brand entrypoint for mAITab.
 * Implementation lives in `mAITab-logo.tsx` (hyphenated) because macOS
 * case-insensitive volumes collapse `MaiTabLogo` ↔ `mAITabLogo`.
 */
export {
  MAITabLogo,
  MaiTabLogo,
  FullLogoWithText,
  IconOnly,
  Monochrome,
  FaviconSVG,
} from "./mAITab-logo";

export { default } from "./mAITab-logo";
